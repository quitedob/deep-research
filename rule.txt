项目的流程是api->service->core(按需)->dao,项目所有相关的资料请阅读根目录的所有txt文件，所有的apikey密钥都在.env文件中,llm必须抽象成一个基类，然后ollama等云服务商根据根目录的txt文件实现对应任务。前端设计原则是:用户操纵控制：用户应始终感觉是自己在主导交互（如提供“撤销”、“退出”功能，避免模式化对话框）。减轻用户的记忆负担：人脑不擅长精确记忆（“识别优于回忆”），界面应提供清晰的导航、默认值、上下文提示。保持界面一致：相同的操作、图标、术语在系统各处应有相同的含义。